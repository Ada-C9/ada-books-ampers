<h2>Books List</h2>
<ul>
<% @books.each do |book| %>
  <li>
    <span class="title">
      <%= link_to book.title, book_path(book.id), class: 'teal-font' %>
    </span>
    by <%= book.author %>
  </li>
<% end %>
<ul>
